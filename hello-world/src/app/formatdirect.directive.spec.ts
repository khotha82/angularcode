import { FormatdirectDirective } from './formatdirect.directive';

describe('FormatdirectDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatdirectDirective();
    expect(directive).toBeTruthy();
  });
});
